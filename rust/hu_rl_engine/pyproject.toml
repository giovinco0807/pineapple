[build-system]
requires = ["maturin>=1.12,<2"]
build-backend = "maturin"

[project]
name = "ofc-hu-rl-engine-native"
version = "0.1.0"
description = "Optional deterministic PyO3 boundary for Regular OFC HU RL correctness"
requires-python = ">=3.11"

[tool.maturin]
features = ["python"]
module-name = "_ofc_hu_rl_engine"
